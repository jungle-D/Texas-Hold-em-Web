{
  "name": "lan-texas-holdem-web",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend",
    "server",
    "shared"
  ],
  "scripts": {
    "dev": "npm run dev -w server",
    "dev:frontend": "npm run dev -w frontend",
    "dev:server": "npm run dev -w server",
    "build": "npm run build -w shared && npm run build -w server && npm run build -w frontend"
  }
}
